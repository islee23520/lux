# LUX AI Tool Adapters

Repository source adapters for AI coding tools. Each subdirectory contains the adapter implementation for a specific AI tool.

> **Note**: These are _source_ adapters that ship with LUX. At runtime, adapters install into tool-specific directories (e.g., `.opencode/plugins/` for OpenCode).

| Adapter | Status | Runtime Target |
|---------|--------|----------------|
| `opencode/` | Verified | `.opencode/plugins/lux-plugin.ts` |
| `codex/` | Scaffolded | TBD |
| `claude/` | Scaffolded | TBD |
| `pi-agent/` | Scaffolded | TBD |
