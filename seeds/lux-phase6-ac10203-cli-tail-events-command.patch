diff --git a/RustGateway~/src/ai_log.rs b/RustGateway~/src/ai_log.rs
index 1a02846..dccf0fc 100644
--- a/RustGateway~/src/ai_log.rs
+++ b/RustGateway~/src/ai_log.rs
@@ -208,6 +208,15 @@ pub fn parse_jsonl_values(reader: impl BufRead) -> Vec<Value> {
         .collect()
 }
 
+pub fn parse_log_entry_line(line_number: usize, line: &str) -> Option<AiLogEntry> {
+    let value = parse_jsonl_line(line)?;
+    Some(AiLogEntry {
+        line_number,
+        timestamp: timestamp(&value).unwrap_or_default(),
+        value,
+    })
+}
+
 pub fn read_log_entries(path: impl AsRef<Path>, filter: &AiLogFilter) -> Result<Vec<AiLogEntry>> {
     let file = File::open(path.as_ref())
         .with_context(|| format!("failed to open AI log {}", path.as_ref().display()))?;
@@ -334,14 +343,7 @@ fn parse_entries(reader: impl BufRead) -> Vec<AiLogEntry> {
     reader
         .lines()
         .enumerate()
-        .filter_map(|(index, line)| {
-            let value = parse_jsonl_line(&line.ok()?)?;
-            Some(AiLogEntry {
-                line_number: index + 1,
-                timestamp: timestamp(&value).unwrap_or_default(),
-                value,
-            })
-        })
+        .filter_map(|(index, line)| parse_log_entry_line(index + 1, &line.ok()?))
         .collect()
 }
 
diff --git a/RustGateway~/src/main.rs b/RustGateway~/src/main.rs
index 9474563..0c0caf1 100644
--- a/RustGateway~/src/main.rs
+++ b/RustGateway~/src/main.rs
@@ -4,10 +4,11 @@ mod server;
 
 use std::{
     fs,
-    io::{BufRead, BufReader, ErrorKind, Read, Write},
+    io::{BufRead, BufReader, ErrorKind, Read, Seek, SeekFrom, Write},
     net::{IpAddr, Ipv4Addr, SocketAddr},
     path::{Path, PathBuf},
     process::{Command as ProcessCommand, Stdio},
+    thread,
     time::{Duration, Instant},
 };
 
@@ -172,9 +173,15 @@ struct AiLogTailArgs {
     limit: usize,
     #[arg(long, default_value_t = false)]
     json: bool,
-    /// Print a bounded snapshot and exit; continuous follow is intentionally non-blocking.
+    /// Continue watching the project log after printing the initial tail snapshot.
     #[arg(long, default_value_t = false)]
     follow: bool,
+    /// Poll interval used while waiting for appended events in follow mode.
+    #[arg(long, default_value_t = 250)]
+    poll_interval_ms: u64,
+    /// Stop after this many newly appended matching events; omit to follow until interrupted.
+    #[arg(long)]
+    max_new_events: Option<usize>,
 }
 
 #[derive(Parser, Debug)]
@@ -705,9 +712,9 @@ fn print_ai_log_list(args: AiLogListArgs) -> anyhow::Result<()> {
         println!(
             "{}",
             serde_json::to_string_pretty(&json!({
-                "projectPath": source.project_path,
-                "path": source.log_path,
-                "pathResolution": source.path_resolution,
+                "projectPath": &source.project_path,
+                "path": &source.log_path,
+                "pathResolution": &source.path_resolution,
                 "count": entries.len(),
                 "entries": entries,
             }))?
@@ -729,9 +736,9 @@ fn print_ai_log_recent(args: AiLogRecentArgs) -> anyhow::Result<()> {
         println!(
             "{}",
             serde_json::to_string_pretty(&json!({
-                "projectPath": source.project_path,
-                "path": source.log_path,
-                "pathResolution": source.path_resolution,
+                "projectPath": &source.project_path,
+                "path": &source.log_path,
+                "pathResolution": &source.path_resolution,
                 "count": entries.len(),
                 "entries": entries,
             }))?
@@ -747,34 +754,155 @@ fn print_ai_log_recent(args: AiLogRecentArgs) -> anyhow::Result<()> {
 
 fn print_ai_log_tail(args: AiLogTailArgs) -> anyhow::Result<()> {
     let source = resolve_ai_log_source(&args.project)?;
-    let filter = ai_log::AiLogFilter {
+    let snapshot_filter = ai_log::AiLogFilter {
         limit: Some(args.limit),
         ..ai_log::AiLogFilter::default()
     };
-    let entries = ai_log::read_log_entries(&source.log_path, &filter)?;
+    let entries = read_ai_log_entries_or_empty(&source.log_path, &snapshot_filter)?;
 
-    if args.json {
+    if !args.follow {
+        if args.json {
+            println!(
+                "{}",
+                serde_json::to_string_pretty(&json!({
+                    "projectPath": &source.project_path,
+                    "path": &source.log_path,
+                    "pathResolution": &source.path_resolution,
+                    "follow": false,
+                    "count": entries.len(),
+                    "entries": entries,
+                }))?
+            );
+            return Ok(());
+        }
+
+        for entry in entries {
+            println!("{} {}", entry.timestamp, entry.value);
+        }
+        return Ok(());
+    }
+
+    stream_ai_log_tail(&source, &entries, &snapshot_filter, &args)
+}
+
+fn stream_ai_log_tail(
+    source: &AiLogSource,
+    snapshot_entries: &[ai_log::AiLogEntry],
+    snapshot_filter: &ai_log::AiLogFilter,
+    args: &AiLogTailArgs,
+) -> anyhow::Result<()> {
+    let mut offset = current_file_offset(&source.log_path)?;
+    let mut line_number = count_log_lines(&source.log_path)?;
+
+    for entry in snapshot_entries {
+        print_ai_log_tail_entry(source, entry, args.json)?;
+    }
+    let mut printed_new_events = 0usize;
+    let follow_filter = ai_log::AiLogFilter {
+        limit: None,
+        ..snapshot_filter.clone()
+    };
+
+    loop {
+        if let Some(max_new_events) = args.max_new_events {
+            if printed_new_events >= max_new_events {
+                return Ok(());
+            }
+        }
+
+        let current_length = fs::metadata(&source.log_path)
+            .map(|metadata| metadata.len())
+            .unwrap_or(0);
+        if current_length < offset {
+            offset = 0;
+            line_number = 0;
+        }
+
+        if current_length > offset {
+            let mut file = fs::File::open(&source.log_path)
+                .with_context(|| format!("failed to open AI log {}", source.log_path.display()))?;
+            file.seek(SeekFrom::Start(offset))
+                .with_context(|| format!("failed to seek AI log {}", source.log_path.display()))?;
+            let mut reader = BufReader::new(file);
+            let mut line = String::new();
+
+            loop {
+                line.clear();
+                let bytes = reader.read_line(&mut line).with_context(|| {
+                    format!("failed to read AI log {}", source.log_path.display())
+                })?;
+                if bytes == 0 {
+                    break;
+                }
+
+                offset += bytes as u64;
+                line_number += 1;
+                if let Some(entry) = ai_log::parse_log_entry_line(line_number, &line) {
+                    if ai_log::filter_entries(vec![entry], &follow_filter)
+                        .into_iter()
+                        .next()
+                        .map(|entry| print_ai_log_tail_entry(source, &entry, args.json))
+                        .transpose()?
+                        .is_some()
+                    {
+                        printed_new_events += 1;
+                    }
+                }
+            }
+        }
+
+        thread::sleep(Duration::from_millis(args.poll_interval_ms));
+    }
+}
+
+fn print_ai_log_tail_entry(
+    source: &AiLogSource,
+    entry: &ai_log::AiLogEntry,
+    json_output: bool,
+) -> anyhow::Result<()> {
+    if json_output {
         println!(
             "{}",
-            serde_json::to_string_pretty(&json!({
-                "projectPath": source.project_path,
-                "path": source.log_path,
-                "pathResolution": source.path_resolution,
-                "follow": args.follow,
-                "count": entries.len(),
-                "entries": entries,
+            serde_json::to_string(&json!({
+                "projectPath": &source.project_path,
+                "path": &source.log_path,
+                "pathResolution": &source.path_resolution,
+                "entry": entry,
             }))?
         );
-        return Ok(());
+    } else {
+        println!("{} {}", entry.timestamp, entry.value);
     }
+    std::io::stdout()
+        .flush()
+        .context("failed to flush AI log tail output")
+}
 
-    for entry in entries {
-        println!("{} {}", entry.timestamp, entry.value);
+fn current_file_offset(path: &Path) -> anyhow::Result<u64> {
+    match fs::metadata(path) {
+        Ok(metadata) => Ok(metadata.len()),
+        Err(error) if error.kind() == ErrorKind::NotFound => Ok(0),
+        Err(error) => {
+            Err(error).with_context(|| format!("failed to stat AI log {}", path.display()))
+        }
     }
-    if args.follow {
-        eprintln!("lux ai-log tail --follow prints a bounded snapshot and exits in this CLI build");
+}
+
+fn count_log_lines(path: &Path) -> anyhow::Result<usize> {
+    let file = match fs::File::open(path) {
+        Ok(file) => file,
+        Err(error) if error.kind() == ErrorKind::NotFound => return Ok(0),
+        Err(error) => {
+            return Err(error).with_context(|| format!("failed to open AI log {}", path.display()))
+        }
+    };
+
+    let mut count = 0usize;
+    for line in BufReader::new(file).lines() {
+        line.with_context(|| format!("failed to read AI log {}", path.display()))?;
+        count += 1;
     }
-    Ok(())
+    Ok(count)
 }
 
 fn print_ai_log_context(args: AiLogContextArgs) -> anyhow::Result<()> {
diff --git a/RustGateway~/tests/gateway_cli_smoke.rs b/RustGateway~/tests/gateway_cli_smoke.rs
index 082b222..cfab157 100644
--- a/RustGateway~/tests/gateway_cli_smoke.rs
+++ b/RustGateway~/tests/gateway_cli_smoke.rs
@@ -71,6 +71,8 @@ fn rust_lux_cli_exposes_batch_mode_help_flags() {
     assert_command_help_contains(&["ai-log", "recent", "--help"], "--project-path");
     assert_command_help_contains(&["ai-log", "recent", "--help"], "--event-type");
     assert_command_help_contains(&["ai-log", "tail", "--help"], "--follow");
+    assert_command_help_contains(&["ai-log", "tail", "--help"], "--poll-interval-ms");
+    assert_command_help_contains(&["ai-log", "tail", "--help"], "--max-new-events");
     assert_command_help_contains(&["ai-log", "context", "--help"], "--json");
     assert_command_help_contains(&["ai-log", "compact", "--help"], "--max-lines");
     assert_command_help_contains(&["unity", "status", "--help"], "--project-path");
@@ -194,8 +196,14 @@ fn ai_log_list_json_filters_only_requested_project_events() {
     assert_command_success(&output, "lux ai-log list project scoped");
     let parsed: Value = serde_json::from_slice(&output.stdout).expect("project-scoped list JSON");
     assert_eq!(parsed["count"], 1);
-    assert_eq!(parsed["entries"][0]["value"]["summary"], "requested project edit");
-    assert_ne!(parsed["entries"][0]["value"]["summary"], "other project edit");
+    assert_eq!(
+        parsed["entries"][0]["value"]["summary"],
+        "requested project edit"
+    );
+    assert_ne!(
+        parsed["entries"][0]["value"]["summary"],
+        "other project edit"
+    );
     assert!(other_project
         .join("UserSettings/LuxAiActionLog.jsonl")
         .is_file());
@@ -263,15 +271,18 @@ fn ai_log_recent_json_uses_shared_project_path_env_and_settings_source() {
 
     assert_command_success(&output, "lux ai-log recent LUX_PROJECT_PATH");
     let parsed: Value = serde_json::from_slice(&output.stdout).expect("project env JSON");
+    let expected_project = project_root.canonicalize().unwrap();
     assert_eq!(
         parsed["projectPath"],
-        project_root
-            .canonicalize()
-            .unwrap()
+        expected_project.to_string_lossy().as_ref()
+    );
+    assert_eq!(
+        parsed["path"],
+        expected_project
+            .join("Settings/events.jsonl")
             .to_string_lossy()
             .as_ref()
     );
-    assert_eq!(parsed["path"], settings_log.to_string_lossy().as_ref());
     assert_eq!(parsed["pathResolution"]["source"], "config");
     assert_eq!(
         parsed["entries"][0]["value"]["summary"],
@@ -340,9 +351,9 @@ fn ai_log_compact_json_atomically_keeps_tail_valid_lines() {
 }
 
 #[test]
-fn ai_log_tail_follow_prints_snapshot_and_exits() {
-    let project_root = create_ai_log_fixture_project("lux-ai-log-tail");
-    let output = Command::new(env!("CARGO_BIN_EXE_lux"))
+fn ai_log_tail_follow_streams_appended_project_events() {
+    let project_root = create_ai_log_fixture_project("lux-ai-log-tail-follow");
+    let mut child = Command::new(env!("CARGO_BIN_EXE_lux"))
         .args([
             "ai-log",
             "tail",
@@ -352,16 +363,81 @@ fn ai_log_tail_follow_prints_snapshot_and_exits() {
             "1",
             "--json",
             "--follow",
+            "--poll-interval-ms",
+            "25",
+            "--max-new-events",
+            "1",
         ])
         .env_remove("LUX_AI_ACTION_LOG_PATH")
-        .output()
-        .expect("run lux ai-log tail --follow");
+        .stdout(Stdio::piped())
+        .stderr(Stdio::null())
+        .spawn()
+        .expect("start lux ai-log tail --follow");
+
+    let stdout = child.stdout.take().expect("tail stdout pipe");
+    let (line_tx, line_rx) = std::sync::mpsc::channel();
+    thread::spawn(move || {
+        let mut reader = BufReader::new(stdout);
+        loop {
+            let mut line = String::new();
+            match reader.read_line(&mut line) {
+                Ok(0) => break,
+                Ok(_) => {
+                    if line_tx.send(line).is_err() {
+                        break;
+                    }
+                }
+                Err(_) => break,
+            }
+        }
+    });
 
-    assert_command_success(&output, "lux ai-log tail --follow");
-    let parsed: Value = serde_json::from_slice(&output.stdout).expect("tail JSON");
-    assert_eq!(parsed["follow"], true);
-    assert_eq!(parsed["count"], 1);
-    assert_eq!(parsed["entries"][0]["value"]["summary"], "opencode review");
+    let snapshot_line = line_rx
+        .recv_timeout(Duration::from_secs(3))
+        .expect("tail snapshot line");
+    let snapshot: Value = serde_json::from_str(snapshot_line.trim()).expect("snapshot JSON line");
+    assert_eq!(snapshot["entry"]["value"]["summary"], "opencode review");
+
+    let log_path = project_root.join("UserSettings/LuxAiActionLog.jsonl");
+    let mut log = fs::OpenOptions::new()
+        .append(true)
+        .open(&log_path)
+        .expect("open AI log for append");
+    writeln!(
+        log,
+        "{}",
+        r#"{"timestampUtc":"2026-05-04T00:00:05Z","actor":"codex","category":"ai-action-log","source":"cli","action":"tail","eventType":"append","summary":"followed codex event"}"#
+    )
+    .expect("append AI log event");
+    log.flush().expect("flush AI log append");
+
+    let appended_line = line_rx
+        .recv_timeout(Duration::from_secs(3))
+        .expect("tail appended line");
+    let appended: Value = serde_json::from_str(appended_line.trim()).expect("appended JSON line");
+    let expected_project = project_root.canonicalize().expect("canonical project path");
+    assert_eq!(
+        appended["projectPath"],
+        expected_project.display().to_string()
+    );
+    assert_eq!(
+        appended["path"],
+        expected_project
+            .join("UserSettings/LuxAiActionLog.jsonl")
+            .display()
+            .to_string()
+    );
+    assert_eq!(appended["entry"]["line_number"], 6);
+    assert_eq!(
+        appended["entry"]["value"]["summary"],
+        "followed codex event"
+    );
+
+    wait_for_child_success(
+        &mut child,
+        Duration::from_secs(3),
+        "lux ai-log tail --follow",
+    );
 }
 
 #[test]
@@ -2446,6 +2522,24 @@ fn rust_lux_auto_detects_unity_path_from_project_version() {
     );
 }
 
+fn wait_for_child_success(child: &mut Child, timeout: Duration, label: &str) {
+    let deadline = Instant::now() + timeout;
+    while Instant::now() < deadline {
+        match child.try_wait() {
+            Ok(Some(status)) => {
+                assert!(status.success(), "{label} exited with status {status}");
+                return;
+            }
+            Ok(None) => thread::sleep(Duration::from_millis(25)),
+            Err(error) => panic!("failed to wait for {label}: {error}"),
+        }
+    }
+
+    let _ = child.kill();
+    let _ = child.wait();
+    panic!("timed out waiting for {label}");
+}
+
 fn assert_command_help_contains(args: &[&str], expected: &str) {
     let output = Command::new(env!("CARGO_BIN_EXE_lux"))
         .args(args)
@@ -2492,11 +2586,7 @@ fn create_ai_log_project_with_log(prefix: &str, log_body: &str) -> std::path::Pa
         .join("Project");
     let lux_directory = project_root.join("UserSettings");
     fs::create_dir_all(&lux_directory).expect("create UserSettings");
-    fs::write(
-        lux_directory.join("LuxAiActionLog.jsonl"),
-        log_body,
-    )
-    .expect("write AI log fixture");
+    fs::write(lux_directory.join("LuxAiActionLog.jsonl"), log_body).expect("write AI log fixture");
     project_root
 }
 
