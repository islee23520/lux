diff --git a/RustGateway~/src/main.rs b/RustGateway~/src/main.rs
--- a/RustGateway~/src/main.rs
+++ b/RustGateway~/src/main.rs
@@ -965,6 +965,11 @@ fn install_skill(args: SkillInstallArgs) -> anyhow::Result<()> {
     } else {
         None
     };
+    if let Some(adaptation) = adaptation.as_ref() {
+        if let Err(error) = ensure_adapted_skill_install_is_compatible(adaptation) {
+            fail_skill_install(args.json, &error.to_string());
+        }
+    }
 
     if target_dir.exists() {
         fail_skill_install(
@@ -1140,6 +1145,31 @@ fn fail_skill_install(json_output: bool, message: &str) -> ! {
     std::process::exit(1);
 }
 
+fn ensure_adapted_skill_install_is_compatible(adaptation: &Value) -> anyhow::Result<()> {
+    let compatibility = adaptation
+        .get("compatibility")
+        .context("adaptation metadata is missing compatibility evaluation")?;
+    let status = compatibility
+        .get("status")
+        .and_then(Value::as_str)
+        .unwrap_or("unknown");
+    if status == "compatible" {
+        return Ok(());
+    }
+
+    let failed_checks = compatibility
+        .get("checks")
+        .and_then(Value::as_array)
+        .map(|checks| failed_compatibility_check_names(checks))
+        .unwrap_or_default();
+    let reason = if failed_checks.is_empty() {
+        status.to_string()
+    } else {
+        failed_checks.join(", ")
+    };
+    bail!("skill compatibility validation failed: {reason}");
+}
+
 fn build_skill_adaptation_metadata(name: &str, source: &str) -> anyhow::Result<Value> {
     if is_url_source(source) {
         bail!("--adapt requires a local skill source directory");
@@ -1262,13 +1291,6 @@ fn build_skill_adaptation_metadata(name: &str, source: &str) -> anyhow::Result<V
     let project_metadata = project_metadata::collect_project_metadata_signals(&project_root);
     let compatibility = evaluate_skill_compatibility(&manifest, &project_metadata)?;
     checks.extend(compatibility.checks.iter().cloned());
-    if !compatibility.compatible {
-        let failed_checks = failed_compatibility_check_names(&compatibility.checks);
-        bail!(
-            "skill compatibility validation failed: {}",
-            failed_checks.join(", ")
-        );
-    }
 
     Ok(json!({
         "schema_version": 1,
diff --git a/RustGateway~/tests/gateway_cli_smoke.rs b/RustGateway~/tests/gateway_cli_smoke.rs
--- a/RustGateway~/tests/gateway_cli_smoke.rs
+++ b/RustGateway~/tests/gateway_cli_smoke.rs
@@ -434,6 +434,12 @@ fn skill_install_project_adapt_writes_metadata_and_info_json_reads_it() {
     assert!(installed_dir.join("references/usage.md").is_file());
     let adaptation_path = installed_dir.join("lux-adaptation.json");
     assert!(adaptation_path.is_file());
+    let stored_adaptation: Value = serde_json::from_str(
+        &fs::read_to_string(&adaptation_path).expect("read installed adaptation metadata"),
+    )
+    .expect("installed adaptation JSON");
+    assert_eq!(stored_adaptation["compatibility"]["status"], "compatible");
+    assert_eq!(stored_adaptation["skill_name"], "smoke-adapt-skill");
 
     let info = Command::new(env!("CARGO_BIN_EXE_lux"))
         .args(["skill", "info", "smoke-adapt-skill", "--json"])
@@ -552,6 +558,10 @@ fn skill_install_adapt_incompatible_compatibility_metadata_exits_1() {
     assert!(parsed["error"]
         .as_str()
         .expect("error string")
         .contains("render_pipeline"));
+    assert!(
+        !project.join(".lux/skills/smoke-adapt-hdrp-only-skill").exists(),
+        "incompatible adapted skill must not be installed"
+    );
 }
 
 #[test]
